<h1>Listing blogs</h1>

<table>
  <tr>
    <th>Url identifier</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%=h blog.url_identifier %></td>
    <td><%= link_to 'Show', blog %></td>
    <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
    <td><%= link_to 'Destroy', blog, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New blog', new_blog_path %>