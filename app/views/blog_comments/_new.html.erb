<%# Guess.
# -params-
# blog: the blog post
-%>

<% form_for(:blog_comment, BlogComment.new, :url => { :controller => 'blog_comments', :action => :create } ) do |f| %>
	<%= f.hidden_field :blog_post_id, :value => blog_post.id %>
	<%= f.text_area :comment, :cols => 60, :rows => 4 %><br /><br />
	<%= f.submit "Submit comment", :class => 'button26' %>
<% end %>
